// if

let isOn = true;

// if (isOn == true) {
//     console.log('The light is on')
// }

// if, by default, checks if things are true

if (isOn) {
    console.log ('The light is on!')
}

if (condition) {
    console.log('words')
}

let one = 1;
let two = 2;

if (one <= two) {
    console.log('one is less than two.')
}
